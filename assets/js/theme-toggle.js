/* theme-toggle.js — deprecated/removed
   Theme is now applied automatically based on the user's system preference
   (prefers-color-scheme). This file retained as a harmless placeholder. */
(function(){ /* noop */ })();